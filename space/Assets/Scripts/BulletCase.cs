using UnityEngine;

public class BulletCase : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    [SerializeField] public BulletData bullet;

   


}
